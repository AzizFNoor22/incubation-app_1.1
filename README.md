
  # EID APP

  Ini hanyalah sebuah prototype, website hanya berfungsi bagian front-end saja, tidak ada data yang bisa di save maupun diubah.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  